<h1>
    {{#link-to 'cart'}}
        Cart
    {{/link-to}}
</h1> 
<span class="icon" style="color: #333;"> 
    <i class="fa fa-shopping-cart" aria-hidden="true"></i>
</span>
{{#if cart.count}}
<span class="tag is-rounded is-success cart-label">{{cart.count}}</span>
{{/if}}